# Frontend API base URL (must be public and reachable from browser)
# Production:
VITE_API_BASE=https://api.flamejourneygame.com

# IMPORTANT:
# For Vercel preview domains (*.vercel.app), always set VITE_API_BASE explicitly.
# Do NOT rely on auto-derived api.<host> for preview URLs.
